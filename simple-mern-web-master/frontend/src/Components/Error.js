import { Alert } from '@mui/material';
import React from 'react'
export default function Error( props ){

//     <Alert sx={{mt: '50%'}} variant={props.variant || 'info'} >
//   {props.children}
//   farhan
// </Alert>
<div style={{mt: '50%'}}> farhjan </div>
}